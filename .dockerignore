# =============================================================================
# DOCKER IGNORE
# =============================================================================
#
# WHY THIS MATTERS:
# Docker sends the entire directory as "build context" to the daemon.
# Without .dockerignore, this includes .git, data, node_modules, etc.
# Result: 1GB+ context vs ~5MB with proper ignore → faster builds.
#
# RULE OF THUMB: Exclude everything except what the Go build needs:
#   - go.mod, go.sum (dependencies)
#   - cmd/, internal/, pkg/ (source code)
#   - config.example.yaml (default config)
# =============================================================================

# Version control
.git/
.gitignore

# Build outputs (we build fresh in Docker)
bin/
dist/
*.exe
*.test
goqueue-amd64

# Runtime data (never ship data in images)
data/

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Test artifacts
coverage.out
*.prof
*.log
*.tmp

# Documentation and website (not needed for build)
website/
docs/
*.md
!README.md

# Deployment configs (not needed inside image)
deploy/terraform/
deploy/kubernetes/
deploy/grafana/
deploy/prometheus/
deploy/testing/

# Terraform state (sensitive)
**/.terraform/
**/*.tfstate*
**/*.tfvars

# Memory bank and CI configs
memory-bank/
.github/

# Client libraries (separate packages)
clients/
api/postman/

# Node.js artifacts (from client libraries)
node_modules/
__pycache__/

# Large directories not needed for Go build
scripts/
